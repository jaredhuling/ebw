% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebw.R
\name{independence_weights}
\alias{independence_weights}
\title{Independence weights for a continuous treatment}
\usage{
independence_weights(
  A,
  X,
  lambda = 0,
  decorrelate_moments = FALSE,
  preserve_means = FALSE,
  dimension_adj = TRUE,
  scaling = "none",
  ...
)
}
\arguments{
\item{A}{numeric treatment vector.}

\item{X}{covariate matrix.}

\item{lambda}{ridge parameter; defaults to \code{sqrt(n)} internally when 0.}

\item{decorrelate_moments}{logical; add equality constraints forcing the
weighted treatment-covariate covariances to zero (solved with \code{osqp}).}

\item{preserve_means}{logical; add equality constraints forcing the weighted
mean of every covariate and of the treatment to equal the unweighted sample
mean (solved with \code{osqp}). Unlike the CRAN \code{independenceWeights} default,
the constraint preserves the WEIGHTED mean under \code{sum(w) = n}, that is
\verb{sum(w_i X_ik) = sum(X_ik)}.}

\item{dimension_adj}{logical; use dimension-adjusted marginal-preservation
weights (default \code{TRUE}).}

\item{scaling}{covariate scaling rule (default \code{"none"}, matching the
validated continuous prototype).}

\item{...}{further arguments passed to \code{\link[=energy_balance]{energy_balance()}}.}
}
\value{
An object of class \code{"ebw"}.
}
\description{
Convenience wrapper that fits independence weights for a continuous
treatment, using the argument names of the mature CRAN interface. Returns an
\code{"ebw"} object so that \code{\link[=predict.ebw]{predict.ebw()}} and the other methods apply.
}

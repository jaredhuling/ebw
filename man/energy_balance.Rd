% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebw.R
\name{energy_balance}
\alias{energy_balance}
\title{Energy balancing and independence weights}
\usage{
energy_balance(
  formula,
  data,
  x = NULL,
  treatment = NULL,
  improved = FALSE,
  estimand = c("ATE", "ATT"),
  treatment_type = c("auto", "binary", "continuous", "multinomial", "grouped"),
  scaling = c("std", "mad", "range", "none"),
  kernel = c("energy", "gaussian"),
  eps = 0.01,
  lambda = NULL,
  dimension_adj = TRUE,
  preserve_means = FALSE,
  decorrelate_moments = FALSE,
  level = NULL,
  max_iter = 20000L,
  tol = 1e-11,
  trt = NULL,
  method = NULL,
  standardize = NULL,
  ...
)
}
\arguments{
\item{formula}{a model formula \code{treatment ~ covariates} (formula interface).}

\item{data}{a data frame containing the variables in \code{formula}.}

\item{x}{covariate matrix (matrix interface).}

\item{treatment}{treatment vector (matrix interface).}

\item{improved}{logical; for a binary or multi-category treatment, add the
between-group energy distance to the objective (default \code{FALSE}). Ignored
for a continuous treatment.}

\item{estimand}{target estimand for a binary treatment, either \code{"ATE"}
(each group balanced to the pooled sample) or \code{"ATT"} (the untreated group
balanced to the treated sample). Ignored for continuous treatments. Only
\code{"ATE"} is supported for a multi-category treatment.}

\item{treatment_type}{one of \code{"auto"}, \code{"binary"}, \code{"continuous"},
\code{"multinomial"} (alias \code{"grouped"}).}

\item{scaling}{covariate scaling rule (\code{.scale_covariates}): one of
\code{"std"}, \code{"mad"}, \code{"range"}, or \code{"none"}.}

\item{kernel}{covariate kernel, either \code{"energy"} (default) or \code{"gaussian"}.}

\item{eps}{ridge parameter for the binary energy-balancing QP.}

\item{lambda}{ridge parameter for the continuous independence-weights QP;
defaults to \code{sqrt(n)} when \code{NULL}.}

\item{dimension_adj}{logical; use dimension-adjusted marginal-preservation
weights for the continuous engine (default \code{TRUE}).}

\item{preserve_means}{logical; add equality constraints forcing weighted
covariate means to match their targets, applied uniformly across all
treatment types. For a continuous treatment it preserves the weighted mean
of every covariate and of the treatment at the unweighted sample mean; for
a binary or multi-category treatment it forces each group's weighted
covariate means to equal the pooled (full-sample) means, so the groups are
mean-balanced exactly. In every case the constrained QP is solved with
\code{osqp} and the out-of-sample predictor gains a dual term linear in the
constraint features.}

\item{decorrelate_moments}{logical (continuous treatment only); add equality
constraints forcing the weighted treatment-covariate covariances to zero.
Also solved with \code{osqp}. Has no binary or multi-category analogue, so a
discrete treatment with \code{decorrelate_moments = TRUE} is an error.}

\item{level}{optional value of \code{treatment} identifying the treated group for
a binary treatment; defaults to the larger / second level.}

\item{max_iter, tol}{FISTA controls.}

\item{trt, method, standardize}{deprecated argument names retained for
backward compatibility. \code{trt} maps to \code{treatment}, \code{method} (\code{"ATE"},
\code{"ATE.3"}, \code{"ATT"}) maps to \code{estimand}, and \code{standardize} (\code{TRUE}/\code{FALSE})
maps to \code{scaling} (\code{"std"}/\code{"none"}).}

\item{...}{additional arguments (currently unused).}
}
\value{
An object of class \code{"ebw"}: a list with the fitted \code{weights}, the
detected \code{treatment_type}, the \code{estimand}, the fitted \code{engine} (carrying
the dual-representation ingredients), the \code{scaling} information, the
\code{call}, the sample size \code{n}, and, for the formula interface, the \code{terms}
and \code{xlevels} needed to rebuild the design matrix at prediction time.
}
\description{
Fits covariate balancing weights for either a binary treatment (energy
balancing weights) or a continuous treatment (independence weights), and
returns an object from which the weight a new unit would receive can be
predicted out of sample via \code{\link[=predict.ebw]{predict.ebw()}}.
}
\details{
The function accepts two calling styles. In the formula style, supply a
\code{formula} whose left-hand side is the treatment and whose right-hand side
gives the covariates, together with a \code{data} frame; the design matrix is
built with \code{\link[stats:model.matrix]{stats::model.matrix()}}, so factors and interactions are
supported. In the matrix style, supply the covariate matrix \code{x} and the
treatment vector \code{treatment} directly. For backward compatibility the older
argument names \code{trt}, \code{x}, \code{method}, and \code{standardize} are also accepted.

The treatment type is detected automatically. A treatment that is a factor,
character, or logical, or a numeric treatment that takes integer-like values
on a small number of distinct levels, is treated as discrete and routed to
the grouped energy-balancing engine: two levels give binary energy balancing,
more than two give multi-category energy balancing. A numeric treatment with
many distinct values is treated as continuous. Use \code{treatment_type} to
override the detection.

With \code{improved = TRUE} the grouped engine adds the between-group energy
distance to the objective, coupling the groups so that they are balanced both
to the pooled sample and to one another. Standard binary energy balancing is
the special case of two groups with the coupling turned off.
}
\examples{
set.seed(20260809)
n <- 200; p <- 4
X <- matrix(rnorm(n * p), n, p)
a <- rbinom(n, 1, plogis(X[, 1] - X[, 2]))
fit <- energy_balance(x = X, treatment = a)
w <- weights(fit)

}
\seealso{
\code{\link[=predict.ebw]{predict.ebw()}} for out-of-sample weight prediction.
}

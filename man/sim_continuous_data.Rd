% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_continuous_data}
\alias{sim_continuous_data}
\title{Simulate confounded data with a continuous treatment}
\usage{
sim_continuous_data(
  n.obs,
  n.vars = 4,
  alpha.norm2 = 0.25,
  AR.cor = 0,
  outcome.coef = NULL,
  tau = 1,
  sd.error = 1
)
}
\arguments{
\item{n.obs}{number of observations.}

\item{n.vars}{number of covariates.}

\item{alpha.norm2}{squared Euclidean norm of the covariate-to-treatment
coefficient vector (confounding strength). The coefficients are set to
\code{sqrt(alpha.norm2 / n.vars)} in every coordinate.}

\item{AR.cor}{AR(1) correlation of the covariates.}

\item{outcome.coef}{coefficient vector for the outcome model.}

\item{tau}{slope of the (linear) treatment effect on the outcome.}

\item{sd.error}{outcome noise standard deviation.}
}
\value{
A list with covariate matrix \code{x}, treatment \code{a}, outcome \code{y}, the
coefficient vector \code{alpha}, and the treatment marginal variance
\code{sigma2.A}.
}
\description{
Generates covariates and a continuous treatment that depends linearly on
them (a linear-Gaussian design), for illustrating and testing independence
weights. With standard normal covariates and treatment noise variance 1, the
treatment marginal variance is \code{1 + alpha.norm2}.
}
\examples{
set.seed(20260809)
dat <- sim_continuous_data(n.obs = 200, n.vars = 4, alpha.norm2 = 0.25)
str(dat)

}

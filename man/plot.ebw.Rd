% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.ebw}
\alias{plot.ebw}
\title{Diagnostic plots for a fitted weighting}
\usage{
\method{plot}{ebw}(
  x,
  type = c("balance", "weights"),
  newdata = NULL,
  weights = NULL,
  bins = 30,
  ...
)
}
\arguments{
\item{x}{a fitted \code{"ebw"} object.}

\item{type}{\code{"balance"} for a Love plot or \code{"weights"} for the weight
distribution.}

\item{newdata}{optional new data; when supplied, the plot reflects the
out-of-sample balance (or predicted weights) on that sample, as in
\code{\link[=balance]{balance()}}.}

\item{weights}{optional weight vector to use instead of the fitted or
predicted weights.}

\item{bins}{number of histogram bins for \code{type = "weights"}.}

\item{...}{unused.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a Love plot of covariate balance before and after weighting, or a
plot of the distribution of the fitted weights.
}
\details{
For \code{type = "balance"} (the default) each covariate contributes a horizontal
pair of points, its imbalance before and after weighting, joined by a
segment. For a binary treatment the imbalance is the absolute standardized
mean difference against the target; for a continuous treatment it is the
absolute distance correlation between the covariate and the treatment. A
dashed reference line is drawn at 0.1. For \code{type = "weights"} the fitted (or
predicted) weights are shown as a histogram.
}
\examples{
set.seed(20260809)
dat <- sim_confounded_data(n.obs = 200, n.vars = 6)
fit <- energy_balance(x = dat$x, treatment = dat$trt)
plot(fit)                       # Love plot
plot(fit, type = "weights")     # weight distribution

}
\seealso{
\code{\link[=balance]{balance()}}
}

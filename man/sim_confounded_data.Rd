% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_confounded_data}
\alias{sim_confounded_data}
\title{Simulate confounded data with a binary treatment}
\usage{
sim_confounded_data(
  n.obs,
  n.vars = 10,
  AR.cor = 0,
  beta = NULL,
  outcome.coef = NULL,
  tau = 1,
  sd.error = 1
)
}
\arguments{
\item{n.obs}{number of observations.}

\item{n.vars}{number of covariates (at least 4).}

\item{AR.cor}{AR(1) correlation of the covariates (absolute value below 1).}

\item{beta}{coefficient vector for the propensity (log-odds) model; recycled
or truncated to \code{n.vars}. Defaults to a decaying alternating-sign pattern.}

\item{outcome.coef}{coefficient vector for the outcome model.}

\item{tau}{true (constant) average treatment effect.}

\item{sd.error}{outcome noise standard deviation.}
}
\value{
A list with covariate matrix \code{x}, treatment \code{trt}, outcome \code{y},
propensity \code{prob.trt}, and the true treatment effect \code{trt.eff}.
}
\description{
Generates covariates, a confounded binary treatment, and a continuous
outcome for illustrating and testing energy balancing weights. The treatment
assignment and the outcome both depend on the covariates, so naive
comparisons are confounded.
}
\examples{
set.seed(20260809)
dat <- sim_confounded_data(n.obs = 200, n.vars = 6, AR.cor = 0.3)
str(dat)

}
